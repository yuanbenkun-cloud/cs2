# 《洞见》美术与收尾接线 · 手把手手册（新手向）

> 配套参考：豆包对话《洞见》设计方案素材与地图分析、`assets/洞见-素材规格表-dsh执行版.md`、`assets/raw/素材2/洞见-素材库使用说明-dsh导入版.md`。
> 项目路径：`F:\游戏\cs2`（Godot 4.7.2，窗口 640×360，像素风 Nearest）。

---

## 0. 先学会这几件事（5 分钟）

1. **打开工程**：双击 `F:\godot\Godot_v4.7.2-stable_win64.exe` → 点 `导入` → `浏览` 选到 `F:\游戏\cs2\project.godot` → `导入并编辑`。
2. **界面**：左上 `场景`（节点树）、右上 `检查器`（属性）、左下 `文件系统`（工程文件）、右下 `输出`（报错）。按 **F5** 运行游戏（主场景=第一关）。
3. **跑关卡**：运行后是 01 关；想直接看 02~05，在 `输出` 或临时脚本里换，或按 Esc 后在 `项目设置→运行→主场景` 临时改成 `res://scenes/guanqia/02_ciqikou.tscn` 再 F5（改完记得改回 01）。
4. **选节点**：运行中看不到；要在 `场景` 面板点开 `LevelRoot` 看层级。想临时试验：双击某场景文件（如 03_zhongshan.tscn）打开编辑器，里面能看到全部节点；鼠标点某个 NPC/背景即可看属性。
5. **素材改动后要"导入一次"**：Godot 打开工程时会自动导入新文件（左下 `文件系统` 里出现同名 .import 即成功）。如果换素材不生效 → 右键该文件 → `重新导入`。
6. **⚠ 最重要**：五关场景（.tscn）**全部由代码生成**（`tools/scene_builder.gd`）。你在编辑器里手工改场景 → 一旦重跑 `scene_builder` 就会被覆盖！
   - **永久修改** → 改代码（下文给的都是代码/数值，粘贴到对应 .gd 文件），再跑一次生成命令。
   - **临时试效果** → 随便改，不满意就重跑生成恢复。

---

## 1. 素材地图（都放在哪、干什么用）

| 素材 | 路径 | 用途 |
|---|---|---|
| 立绘（对话用） | `assets/portraits/*.png` | 对话框两侧立绘（陈默+11 NPC） |
| NPC 待机动画条 | `assets/npc_anim/*.png` + `meta.json` | 场景内 NPC 站立动画 |
| 主角动画条 | `assets/player_frames_norm/{idle,walk,jump,interact}_n.png` | 主角待机/走/跳/交互 |
| 背景（每关 7 件） | `assets/raw/素材2/背景/0X关名/` | 远景主背景/中景无缝/地形无缝 |
| 场景摆件 | `assets/objects/{stilt,kiln,teahouse,dock}.png` | 吊脚楼/窑炉/茶馆门面/码头 |
| 光影贴图 | `assets/raw/素材2/光影/{glow_soft_*}.png` | 灯光光晕（透明，Add 混合） |
| 动态帧 | `assets/raw/素材2/帧表/场景动态/{江水_3帧,灯笼摆动_3帧,窑火_4帧}.png` | 江面/灯笼/窑火循环动画 |

---

## 2. 任务 A：光影贴图接线（编辑器手动法 + 代码法）

**目标**：把 `glow_soft_暖白.png` 等挂到灯笼/路灯上，画面立刻有光感。

### 法 1：编辑器手动（推荐先练手，改的是 01 场景副本即可）
1. 双击 `res://scenes/guanqia/01_hongyadong.tscn` 打开。
2. 左侧 `场景` 点中某个灯笼节点（名字 `OBJ_Lantern`）。
3. 顶部菜单 `节点→添加子节点`（或右键→`添加子节点`），搜索 `Sprite2D` 回车。
4. 选中新 Sprite2D → 右侧 `检查器`：
   - `Texture`：拖入 `assets/raw/素材2/光影/glow_soft_暖白.png`
   - `Scale`：填 `3, 3`（大光晕）
   - `Modulate`：暖黄色 `1, 0.9, 0.6, 0.7`（也可点色块选）
   - `Z Index`：改成 `-1`（放在灯笼后面）
5. 想让它发光柔和：`检查器` 里搜 `Material` → `新建 CanvasItemMaterial` → `Blend Mode` 选 `Add`。
6. 复制粘贴该 Sprite2D（Ctrl+D）到第二只灯笼下。
> 记得：这只改动了当前 .tscn；要永久保留就照"法 2"把同样逻辑加进 builder。

### 法 2：代码法（永久，推荐）
打开 `F:\游戏\cs2\tools\scene_parts\build_01.gd`，找到灯笼那段（搜 `# --- 装饰：灯笼 ---`），在它的 `for lp in ...` 循环里、`root.add_child(lsp)` 之前插入：

```gdscript
		# 灯笼光晕（Add 混合）
		var glow: Texture2D = load("res://assets/raw/素材2/光影/glow_soft_暖白.png")
		if glow != null:
			var gsp := Sprite2D.new()
			gsp.texture = glow
			gsp.scale = Vector2(3, 3)
			gsp.modulate = Color(1.0, 0.9, 0.6, 0.7)
			gsp.z_index = -1
			root.add_child(gsp)
			gsp.position = Vector2(lp[0], lp[1] + 14)
```

（03 古镇关同理，把对应 `build_03.gd` 的灯笼循环里也插一份；02 窑火用 `glow_soft_橙火.png`、04 用 `glow_soft_煤油黄.png` 更配时代。）
保存后跑第 4.2 节的重建命令。

---

## 3. 任务 B：动态帧接线（江水 / 灯笼 / 窑火）

引擎自带面板即可切帧，不用写代码。

1. 以"江水"为例：双击 `res://scenes/guanqia/01_hongyadong.tscn`。
2. 右键 `LevelRoot` → `添加子节点` → 搜 `AnimatedSprite2D` 添加。
3. 选中它 → `检查器` → `Sprite Frames` → `[空]` 下拉 → `新建 SpriteFrames`。
4. 点下方弹出的 `SpriteFrames` 面板（底部新窗口）：
   - 左侧动画名 `default` 改成 `water`；
   - 点 `添加帧` 旁的小箭头 → `从精灵表添加`（Add frames from Sprite Sheet）；
   - 选 `assets/raw/素材2/帧表/场景动态/江水_3帧.png`；
   - 弹出切分窗口：`Columns=3`、`Rows=1`（灯笼也是 3 列；窑火 4 列）；
   - 自动生成 3 帧 → `确定`；右侧把 `Speed` 改 6，勾上 `Loop`（循环）。
5. 回到 `检查器` 调位置/大小：江水放在江面位置，`Scale` 视需要填（如 2,2）。
6. 同样的方法给灯笼（替换现有静态灯笼贴图时，把静态 Sprite2D 隐藏或删掉）、02 关窑火做动画。
> 永久保留：把同样的"从精灵表切帧"逻辑写进代码很啰嗦，建议先用上面 SpriteFrames 面板在场景里做，满意后告诉我"哪一关哪个位置"，我帮你落成 builder 代码。

---

## 4. 任务 C：背景层微调（最常要动的数值，全在 1 个文件）

打开 `F:\游戏\cs2\tools\scene_parts\build_common.gd`，往下翻到函数 `func add_scene_art(...)`。想调什么就改对应数字，然后跑重建命令即可（改完会自动刷所有关）。

| 想调 | 找这行 | 改哪里 |
|---|---|---|
| 远景（天空氛围）高低/大小 | `var s := 360.0 / float(...)` | 360 = 贴图显示高度；`-40.0` = 顶部贴图起始 y |
| 远景左右不铺满/露缝 | `motion_mirroring = Vector2(...)` | 一般不用动（自动按贴图宽） |
| 中景层高度/位置 | `var s := 200.0 / ...` 与 `70.0` | 200=显示高；70=顶边 y（越大越往下/贴近地面） |
| 地面贴图带高度 | `var s := 130.0 / ...` | 130=显示高（贴地、盖住原纯色地） |
| 地面贴图是否铺满 | `3300.0 / step_x` | 3300=覆盖宽度，一般别动 |
| 某关不用远景/中景 | 对应关文件夹 | 删/改名即可（函数找不到文件就自动跳过） |

摆件大小/位置在各自 builder 末尾，搜 `add_object_behind`：
```gdscript
b.add_object_behind(ground, "res://assets/objects/kiln.png", 1000.0, 180.0)
#                                               （x 坐标）（显示高 180）
```
数字 = 摆件底部中心所在世界 x（游戏里 1 格=1 像素，屏幕宽 640）、显示高度像素。想放大/放右/放左改这两个数即可。

---

## 5. 常用"重建 + 验证"命令（复制粘贴进 PowerShell 即可）

**重建五关场景（改动 builders 后必跑）：**
```powershell
& "F:godotGodot_v4.7.2-stable_win64.exe" --headless --path "F:游戏cs2" --script res://tools/scene_builder.gd
```
看到每行 `[PASS] build_0X` 且最后 `[RESULT] PASS` = 成功。

**验证关卡内容完整：**
```powershell
& "F:godotGodot_v4.7.2-stable_win64.exe" --headless --path "F:游戏cs2" --script res://tools/verify.gd
```

**重新生成美术素材**（改过生成器 `tools/gen_art.gd` 才需要）：把上面命令的 `scene_builder.gd` 换成 `gen_art.gd` / `npc_frames.gd` / `normalize_frames.gd`。
素材（PNG/JPG）放进工程后：**先在编辑器打开一次工程**让它导入（自动生成 .import），否则运行不显示。

> 运行游戏测效果：编辑器按 F5（有界面）；命令行跑的是无头验证，不出窗口。

---

## 6. 立绘 / 对话框微调

- **新增说话人立绘**：把透明 PNG 放进 `assets/portraits/`（名字随意，英文），然后在 `F:\游戏\cs2\scripts\ui\dialogue_panel.gd` 里 `PORTRAITS` 字典加一行：
```gdscript
	"新角色名": ["res://assets/portraits/xxx.png", false],   # false=NPC 放左；true=陈默放右
```
- 说话人名字必须和 JSON 对话里的 `speaker` 完全一致。
- **对话框位置/字号**：同一文件顶部 `BOX_TOP := 270.0`（框顶 y）、立绘大小 `PT_W/PT_H`；文字 `font_size 12`、名字 `13`——像素字建议用 12 的倍数最锐（12/24）。
- **主角动画速度/帧**：`assets\scripts\player\hero_anim.gd` 顶部 `SHEETS` 的 `fps`。

---

## 7. 常见问题自查

1. **图不显示** → 先在编辑器打开一次工程导入素材；右键文件→重新导入。
2. **改了 builder 没变化** → 忘跑"重建命令"（第 5 节）。
3. **改场景手工丢了** → 正常：那是代码生成的，重跑即恢复；永久改动请走代码。
4. **报错看不懂** → 复制 `输出` 面板整段贴回对话，我帮你查（别自己乱删）。
5. **动画"糊"** → 检查该节点没被平滑缩放：保持 `Texture Filter`=Nearest、缩放用整数或与像素字体相同的 2 倍。
6. **背景某关很奇怪（叠穿/太高）** → 第 4 节数值微调，或干脆告诉我现象我来调。

---

## 8. 你现在可以自己做的小清单（从易到难）

- [ ] 01 关挂光晕（法 2 复制代码 → 重建 → F5 看）
- [ ] 江水/窑火 动态帧（编辑器 SpriteFrames 面板）
- [ ] 各关背景数值微调到顺眼
- [ ] 摆件大小/位置微调
- [ ] 新增一句带立绘的对话（JSON+PORTRAITS 各一行）
- [ ] 主角动画速度调顺手
- [ ] 遇到任何一步卡住：截图或贴报错，回来问我

> 豆包那份《设计方案》可在左侧参考剧情/氛围目标；技术细节以上文+两份额外 md 为准。