# DEV_LOG（《洞见》Godot 执行日志）

- 项目根：F:\游戏\cs2
- Godot 可执行文件（$godotExe）：F:\godot\Godot_v4.7.2-stable_win64.exe（4.7.2 stable，满足 ≥4.3，使用 TileMapLayer 体系）
- 规格备份：SPEC.md（与 F:\游戏\cs2\洞见-Godot-DeepSeekHarness-执行规格书.md 同源）
- 启动方式：一律 --headless；工具脚本 extends SceneTree

| Phase | 结果 | 日志 | 备注 |
|---|---|---|---|
| Phase 0 | PASS | phase0_verify.log | 目录/project.godot/autoload空壳/builder+verify骨架；init 主场景未生成属预期（Phase1 后复跑干净） |
| Phase 1 | PASS | phase1_build/verify/init.log | Player/Interactable/LevelManager/Checkpoint/SceneTransition/CameraRig；build_01 最小骨架；02-05 先建桩保证 scene_builder 幂等 |
| Phase 2 | PASS | phase2_build/verify/init.log | DialogueSystem+DialoguePanel+JSON+NPC_Test；验证含 JSON 解析 |
| Phase 3 | PASS | phase3_build/verify/init.log | 01 完整：HeartSystem/NPC_FlyerLady/CrowdArea+掩体/OBJ_InfoBoard/OBJ_LooseTile(→02)/Checkpoint/HUD心形；实现细节：跨脚本用路径 extends+运行时 /root 查 autoload（headless 无类缓存的偏离，记录） |
| Phase 4 | PASS | phase4_build/verify/init.log | 02 完整：ForgeSequence(HEAT→QUENCH→CHISEL×3,180s)/OBJ_Wall色块裂开/OBJ_Wood抬不动/糊粥/老匠人/阿明/Checkpoint/UI_Timer+UI_Notice；对话 JSON level2 |
| Phase 5 | PASS | phase5_build/verify/init.log | 03 完整：GameState 货物完整度(stolen/cheated/robbed/safe, tier3档) + ChoiceSystem + 四 NPC（老掌柜/胖掌柜/帮工/老周验货3档→通关04）+ OBJ_GoodsBag；进入03自动 reset_goods |
| Phase 6 | PASS | phase6_build/verify/init.log | 04 完整：GroupFollower(6人环形缓冲跟随)+BombWarning(预警/高亮/爆炸判定→fail)+极暗+PointLight2D跟随+洞中途Checkpoint+OBJ_Exit(→05) |
| Phase 7 | PASS | phase7_*/phase9_verify.log | 05 完整：步行模拟 + OBJ_InfoBoard/NPC_OldMan/OBJ_ViewSpot/OBJ_PhotoSpot(拍照二选一→结尾独白→重开01)；传单阿姨内心OS |
| Phase 8 | PASS | phase8_*/phase9_verify.log | 背景系统：每关 5 层 ParallaxBackground(BG_Sky..BG_Fore, 江水 motion_mirroring) + WorldEnvironment(glow/adjustments) + LightRig(CanvasModulate时代色调+点光源)；swing/path_mover/light_rig 脚本就位 |
| Phase 9 | PASS | phase9_build/verify/init.log | 打磨与总验收：builder 二次运行幂等 PASS；verify 全场景 PASS；音频占位 README；TODO P0/P1 清零；可选导出失败（模板缺失，按规格降级并记录） |

## 实现偏离与说明（均已按 0.5 记录）
1. headless --script 运行不重建全局类缓存：跨脚本统一用路径 extends / load + 运行时 get_node_or_null("/root/X") 取 autoload（SPEC 6.x 接口行为不变）。
2. PackedScene.pack 需整树 owner=root，save_scene 内递归赋 owner（否则子节点不序列化）。
3. 场景 02–05 在未实现前先建桩，保证 scene_builder 全量幂等；随 Phase 逐个替换为真实现。
4. fail/respawn 采用"暂停1.5s显示文案→重载当前场景"（等价于关卡重置/存档点重来，checkpoint 接口保留）。
5. 相机 shake 接口放 scripts/systems/camera_rig.gd（spec 未给路径的合理实现细节）。
6. 4.7 移除 Environment.vignette_*：Phase 8 环境省略 vignette（记录后按可用 API 实现）。
7. 可选导出：官方 Export Templates(4.7.2) 未安装 → 记录 TODO，验收降级（冒烟全 PASS + 编辑器可打开）。

## Codex 接手改造（2026-09-10）
- 五关主角从各关卡内联节点迁移为 `scenes/renwu/zhujue/zhujue.tscn` 的 PackedScene 实例，碰撞、相机与表现层改为单点维护。
- 主角场景新增 `VisualPivot + AnimationPlayer`，统一提供起跳拉伸、落地压缩回弹、交互前倾三段表现动画；动画只影响视觉枢轴，不影响 CharacterBody2D 物理状态。
- 修复 `player_animator.gd` 原先从错误节点层级查找主角精灵的问题；方向与反馈动画现在从角色根节点解析。
- 清理游戏代码中的变量遮蔽警告；McpInteractionServer 的调试警告暂保留，便于后续截图和输入自动化。
- 第一、二、三、五关剧情 NPC 已迁移为 `scenes/renwu/npc/*` PackedScene 实例；共享 NPC 场景新增 `VisualPivot`、呼吸摇摆 AnimationPlayer 和带 E 字样的浮动交互提示。
- 修复 `save_scene()` 递归重设子场景内部 owner 导致实例子节点重复序列化的问题；关卡专属实例子节点改为显式 owner。
- 第一轮视觉修复：背景改为镜像铺带并增加天空兜底；相机加入世界边界与更合理的纵向偏移；主角/NPC 放大并使用共享轮廓 shader；第一关出生点移入安全画面区。
- 修复地形贴图误用世界坐标作为 Ground 局部坐标的问题，五关地形视觉起点由世界 x=200 校正为 x=-700。
- 第三至第五关完成实机视觉巡检；第五关晨光中景裁去大面积空天空后再平铺，原有两条白色断层改为连续晨雾纵深。
- 第五关三个交互点由纯色色块升级为原生节点组合的木牌、观景镜与相机造型，并统一使用带 `E` 字样的前景提示。
- 第四关六名护送群众迁移为 `binanzhongqun.tscn` PackedScene 队列，调整初始间距；轰炸警告改为低强度红色脉冲，落点标记提升到地形前景层。
- 修复 `bomb_warning.gd` 在场景装配期间直接向 current_scene 添加节点导致的 Godot 4.7 启动错误。
- 启动页修复零尺寸根 Control 导致的整体裁切，并重做为夜景首屏、章节卡片、明确操作提示与原生 `AnimationPlayer` 呼吸动效。
- 场景转场改为 await 淡入 → 同步切换 → 等待新场景装配 → 淡出并自销毁，避免大场景加载后遮罩残留。
- 修复主角脚本查错交互区节点名导致 `nearby` 永远为空的问题；提示显示改为与主角交互 Area 的 enter/exit 一致，并提升提示绘制层级。
- 对话面板重排为固定 640×360 的底部电影式布局，加入清晰边框、姓名/正文层级、双侧立绘与 `E 继续` 提示。
- 清理 `McpInteractionServer` 的变量遮蔽与枚举赋值警告，便于后续运行日志只暴露真实问题。

## Codex 分层视差舞台（2026-09-11）
- 新增共享 `ParallaxStage` 场景，五关统一使用 `BG_Sky / BG_Far / BG_Mid / BG_Near / BG_Fore` 五张独立景片；水平速度依次为 `0 / 0.08 / 0.30 / 0.62 / 1.10`。
- 天空和远景压低对比度，中景承载关卡主体轮廓，近景与前景使用时代配色的纵向雾带；第五关中景裁去素材顶部空白天空，避免重复接缝。
- 可行走地形、碰撞与交互对象继续留在世界节点中并以 `1.0` 速度移动，背景不承担碰撞语义。
- 新增 `assets/map/parallax_manifest.json`，固化 640×360 舞台、3200 像素横向长度、层级顺序、滚动速度、重复策略及碰撞来源。
- 实机移动对照验证：同一次横向移动中，远景约位移 35 px、中景约位移 130 px，确认不是静态整图叠放，而是真正的分速视差。
- 修复第五关奖励老人素材被当作单帧、一次显示六个人的问题；新增可重复执行的 `tools/normalize_old_man_sheet.py`，输出等宽六帧动画表并更新元数据。

## Codex 正式素材替换首轮（2026-09-12）
- 建立 `docs/art-direction-brief.md` 与 `assets/production/asset-manifest.json`，统一雾重庆、电影式像素绘制、低饱和时代色彩、深棕轮廓和 64 px 实机可读性标准，并保存生成提示词与验收状态。
- 废弃运行时仍误用的旧主角 `assets/player_frames*`：陈默改用正式的待机 4 帧、行走 6 帧、跳跃 4 帧、交互 4 帧透明动作素材，由 `hero_anim.gd` 组装为 Godot `SpriteFrames`。
- 主角动画按脚底锚点与统一身高校准；行走循环和待机已实机确认，动作贴图经过透明通道、空帧、边缘接触、身体尺度及锚点漂移检查。
- 对话系统中的陈默立绘替换为与动作素材同一身份特征的新透明半身像；实机确认人物位于右侧、不遮挡正文、无棋盘格或黑底烘焙。
- 第一、五关历史木牌由几何色块替换为共享正式素材；第五关观景镜和老式相机也改为独立透明道具，并抬高交互提示避免遮住轮廓。
- 清除第五关误挂在拍照点附近的旧 `assets/objects/dock.png` 大方块贴图；当时暂留的旧洪崖洞景片已在下一阶段的五关背景重制中全部退出运行时。

## Codex 五关背景美术重制（2026-09-12）
- 更正上一轮只做结构分层、未真正替换背景美术的问题；五关全部切换到 `assets/production/backgrounds/<era>/sky|far|mid.png` 新景片，视差系统不再读取 `assets/raw/素材2/背景` 的旧主背景和旧中景；旧目录目前只提供与碰撞同速的地面纹理。
- 每关均为真正独立的三张图片层：不透明天空、透明远景、透明中景；Godot 近雾与前景雾作为另外两层，地面、碰撞、NPC 和交互物继续保留在世界节点中。
- 01 改为现代洪崖洞夜雨、河谷桥梁与错落吊脚楼；02 改为烟霞窑场山村；03 改为暴雨下的河镇纵深；04 改为不规则洞窟支撑与分叉隧道；05 改为晨雾山城、桥谷和崖壁吊脚楼。
- 五套源景片统一规范化为 1280×720，运行时以 640×360 显示；远景/中景透明通道和尺寸全部通过自动检查，并保存原图、提示词、分层预览及生成来源。
- 移除第四关仍覆盖在新洞窟背景上的两条旧纯色洞壁横条；第一关进行出生点与移动后双截图验证，其余四关均完成实机画面检查。

## 2026-09-13 — 第一、二关玩法重构与交互物正式化

- 第一关从四个复制追兵改为传单阿姨本人持续追赶；加入等待/追逐/命中恢复状态、距离橡皮筋速度、三心容错、击退与轻震屏。
- 增加追逐压力条；即使玩家错过开场对话，越过触发线也会自动进入追逐，不会漏掉关卡核心玩法。
- 四组静态人墙改为一次性的动态路人阻挡：玩家接近时先显示 `!` 预警，随后从街边挤入封路约一秒，再主动退场。设计目标是制造节奏题，而不是永久卡路。
- 第二关改为 3 轮 × 3 工序的移动条判定。烤热较慢且宽、淬水中速且目标偏移、凿击最快且窄；黄色为完美区。成功推进工序，完美加时并给 100 分，普通成功 65 分，失误不推进且扣 5 秒。
- 新增总分、连击、最高连击、105 秒限时与分级反馈；凿击成功触发岩壁裂纹和震屏。
- 用正式透明素材替换松动地砖、糊粥、木料、岩壁色块占位；生成记录与技术信息写入 `assets/production/asset-manifest.json`。
- 自动回归 `tools/test_gameplay_v3.gd` 覆盖追兵移动、追逐 HUD、路人入场、移动条开启、完美命中与失误惩罚；全部通过。
- 原生 640×360 视觉检查：`Build/Game/codex_gameplay_v3_chase.png`、`codex_gameplay_v3_forge.png`、`codex_gameplay_v3_props.png`、`codex_gameplay_v3_tile.png`。
## 2026-09-13 — 五关剧情重写

- 主线统一为“从只看景，到看见支撑城市的人”：第一关建立陈默只想抢机位的缺点；第二关用守窑与抗水让他理解生计；第三关把责任落在一包关系十几户工钱的染布上；第四关让他承担带六人走出防空洞的责任；第五关回到同一条街完成态度反转。
- 传单阿姨不再只是追逐工具：补足“差两单”的工作压力，并在第五关以提醒湿滑石板回应第一关，形成首尾闭环。
- 第三关由无提示陷阱改为线索型选择。老掌柜明确给出“双环绳结、绿色木牌”规则；最佳解不是盲信或拒绝所有人，而是核对身份后合作。
- 第四关增加 `1941 · 重庆防空洞` 自动开场，交代唯一灯源和“一个都不能少”的护送目标；开场结束后正式启动此前遗漏的 `GroupFollower.start_following()`。
- 增加关间叙事字幕，连接岩壁、货包、防空警报和现代晨光，减少五关之间的跳切感。
- 结局回收窑火、绳结、煤油灯与传单阿姨；拍照/不拍照保留不同表达，但共享同一主题结论。结局后返回标题页，不再莫名循环到第一关。
## 2026-09-13 — 第四关可见落弹与耐久挡板

- 将原来的“地面红条倒计时后直接爆炸”改为三段式威胁：红色落点预警、导弹从画面顶部加速下落、撞击挡板或地面爆炸。
- 路线中加入三块顶置挡板，每块覆盖约 184 像素并有 2 点耐久。首次命中显裂纹，第二次损毁；损毁后导弹会继续穿过该位置。
- 挡板成功拦截时只产生较轻震屏与高处爆炸；空隙落弹才以 54 像素半径检查玩家和六名群众。
- 跟随队伍采样从 0.5 秒缩短到 0.2 秒、间隔历史从 3 个采样缩为 1 个，并提高后排速度，使全队能实际进入同一块挡板下方。
- 新增顶部状态提示，明确区分预警、下落、挡板拦截和地面爆炸。
## 2026-09-13 — 其余关卡玩法状态化重构

- 第三关新增强制护货流程：老掌柜接货 → 茶馆信息边界 → 核验帮工 → 老周验货；禁止绕过流程直达结算。
- 新增常驻货物完整度条和阶段目标。对话分支导致货损时立即变色、跳字并轻震屏，安全判断同样给出正反馈。
- 第五关新增四块“晨光记忆”收集：木牌文字、建造者、谋生者、清晨江景。未完成时观景台反馈缺失项目，4/4 后才解锁结局。
- 第二关的同一工序随轮次递增 11% 游标速度、递减 9% 目标宽度，形成明确的三轮难度曲线。
- 保留第一关的纯追逐定位，避免每关堆叠同一种收集/数值系统；终点继续用松动地砖和叙事转场完成节奏收束。

## 2026-09-13 — 五关原创配乐与环境声

- 新增五条 24 秒主题配乐与五条 24 秒环境循环，全部由 `tools/generate_audio_loops.py` 数值合成，不使用外部采样；生成参数和来源记录保存于 `assets/audio/generated/audio-manifest.json`。
- 声音按关卡自动路由并交叉淡入淡出：夜雨冷钟、窑火低鼓、古镇五声音阶、防空洞心跳警报、晨光上行动机分别对应五关情绪曲线。
- `AudioManager` 增加常驻 Music/Ambient 播放器；对话出现时自动压低音乐 7 dB、环境声 3 dB，对话结束后平滑恢复。
- ESC 菜单扩展为总音量、音乐、环境声、音效、界面音五路独立控制，并保存到 `user://settings.cfg`。
- `tools/test_audio_v1.gd` 覆盖 10 条资源导入/时长、五关路由与五个滑杆；连同既有五组回归共 6 组全部通过。Windows 发布版重新导出并确认可启动、无 9090 调试端口。

## 2026-09-13 — 角色与地面接触修正

- 移除主角和 NPC 运行时叠加的 7 像素黑色轮廓 shader；角色素材自身已有清晰轮廓，额外描边在暗背景中会形成黑色包边。
- 为角色增加独立于呼吸/动作动画的低透明椭圆接触阴影，既保留前后景可读性，也避免阴影随身体悬浮。
- 从五张旧地形源图的真实可行走表面重新裁切运行时地面，并适度降低亮度与饱和度；地面贴图顶线现在与世界碰撞面 `y=240` 一致。
- 第一至第五关的静态剧情 NPC 与防空洞六人队伍统一校正到 `y=240`，解决 Area2D 角色不会受重力影响而停在旧 `y=210~215` 的悬空问题。
- 五关均完成 640×360 实机截图检查；新增 `tools/test_visual_grounding_v1.gd` 固化地面资源、材质、接触阴影和 NPC 脚底位置。

## 2026-09-13 — NPC 左右朝向

- 在 `Skinner` 中为所有正式 NPC 动画建立统一朝向记录；只翻转 `SkinAnim`，交互提示、碰撞与脚下阴影不会镜像错位。
- 追逐者和防空洞跟随队伍优先按实际横向位移转身；静态剧情 NPC 在玩家进入 300 像素范围后自动面向玩家。
- 移除传单阿姨脚本中对废弃 `Visual` 节点的无效翻转逻辑，朝向现在作用于实际运行中的 `AnimatedSprite2D`。
- 新增 `tools/test_npc_facing_v1.gd`，覆盖静态左右站位、追逐折返和队伍双向移动。
- NPC 待机帧率由 6 FPS 统一降低为 4 FPS，使人物呼吸和衣摆变化更沉稳；游戏逻辑、物理与朝向检测仍按正常运行帧率更新。

## 2026-09-13 — 对话输入、第一关重试与沉浸式转场

- 对话推进改为消费离散输入事件：普通 E 键可立即补全文字或进入下一句，键盘自动连发事件会被忽略，同一次按下不会跨过两句。
- 第一关失败时不再恢复到追逐触发器之后的检查点，而是从街口完整重开，确保传单阿姨与追逐触发器重新出现并可启动。
- 第二至第五关插画文字由客观剧情总结改为陈默当下的感官与行动：雨水、窑锤与船号、布包勒手、警报与落土、清晨扫帚声依次串起旅程。
- 时空裂隙不再由转场旁白直接解释；来源延后到第五关，由老人关于“旧事寻找一双肯停下来的眼睛”的说法自然回收。
- 第二关石壁插画将操作提示写进老匠人的现场叮嘱，在不打断代入感的前提下交代烧、淬、锤的玩法因果。
- 新增第一关失败重试回归，并加入真实 E 键输入测试；全部 13 组游戏回归与结构验证通过。

## 2026-09-14 — 前四关出口时空门与最终关修正

- 第一至第四关终点统一改成贴地、带环形流光与粒子的动态时空门；第五关按设计保留照相点，不放出口传送门。
- 第二关完成九次锻造、第三关完好交货后只开启传送门，玩家实际穿过门才进入下一关，避免任务完成后突然切场景。
- 第四关失败重来强制返回初始出生区域，不再恢复中途检查点；导弹与挡板状态也随场景完整重置。
- 第五关传单阿姨改用实色透明裁切材质，角色本体不再被晨雾和灯光稀释成半透明。
- 每段新对白开始前清空左右立绘缓存，旁白也会明确清空两侧，修复拍照对白左侧残留传单阿姨的问题。
- 完成前四关传送门、第五关人物与对白的 640×360 实机视觉检查；难度专项、原玩法、其他关卡与结构验证全部通过，并重新导出 Windows 发布版。

## 2026-09-14 — 逐格漫画叙事与关卡动机链

- 将原先自动播放的序章卡片改成 2×2 逐格漫画：鼠标左键、E 或空格每次只揭开一格，四格全部显示后再按一次进入游戏。
- 新序章明确陈默急于洪崖洞打卡、为避开主路人群抄小路，却仍遇见传单阿姨；他嫌阿姨麻烦而逃跑，直接接入第一关追逐。石头机关延后到第一关后段首次出现。
- 第二至第五关各增加四格关间漫画，同时保留并裁切使用原有正式插画：山洪为什么必须凿壁、染布为什么必须完整送达、十几户工钱为何受影响、空袭中引路人为何把六人托付给陈默、古墙为何让他走完前人的路。
- 第一关对白承接序章：传单阿姨追赶源于陈默嫌烦逃走；终点木牌才首次提示墙脚石扣与可疑近路。
- 新增 `StoryComic` 原生 UI 与专项测试；完成序章及四个关间漫画的 640×360 实机截图检查，剧情、输入、重试、玩法与场景结构回归全部通过。

## 2026-09-15 — 漫画加载环境声与逐关 BGM 衔接

- 为第二至第四章新增三条 24 秒原创过场环境循环，均由 `tools/generate_audio_loops.py` 数值合成、不使用外部采样：第二章是近景窑火、木炭爆裂与远处锤击；第三章是旧时布市底噪、布匹抖动和左右交错的远处叫卖；第四章是起伏防空警报、低空轰鸣与由远及近的爆炸。
- `AudioManager` 新增独立 `StoryAmbiencePlayer`，漫画出现时淡入、结束时淡出并释放，使用 Ambient 音量滑杆；不会替代五关原有 BGM 与常驻环境声。
- 漫画揭格增加情境短音效：窑场关键格触发锻打声，染布关键格触发布料声，进入 1941 年的两格依次触发警报和爆炸。原有每关专属 BGM 会在场景切换时提前接入漫画，进入玩法后自然延续。
- 音频清单由 10 条扩展为 13 条原创循环；新增 `tools/test_story_audio_v1.gd`，覆盖资源时长、章节路由、播放与淡出清理、逐格音效标记。

## 2026-09-15 — 第一幕漫画重新构图

- 第一幕四格不再共用一张远景并在运行时叠加人物贴片，改为四张独立的完整画格；角色身份继续使用已确认的主角和传单阿姨正式素材。
- 镜头节奏重排为手机赶路近景、转入窄巷的强透视镜头、递传单与拒绝的双人对峙、传单飞散的斜向追逐；加入前中后景遮挡、雨线、速度线和冷暖光区分。
- 四格统一补上湿石板前景、透视接缝、积水反光和脚下接触阴影，解决旧漫画人物悬空与站桩感；底部仍保留低细节暗区供 Godot 字幕覆盖。
- 新增可重复生成脚本 `tools/build_prologue_comic_v2.py`，并更新漫画回归测试，确保四格使用独立成片、不再二次叠加角色。

## 2026-09-15 — 环境声实录替换与第四关警报修复

- 确认旧第四关警报振幅过低，又叠加 Ambient 总线与对话 ducking，实机几乎不可辨；每轮落弹的 `alert.ogg` 只是短提示音，也不能承担防空警报氛围。
- 第二关窑火、第三关布市、第四关防空警报全部替换为 CC0 真实录音；来源、作者与许可记录在 `assets/audio/field/LICENSES.md`。
- 三条真实环境录音直接作为关卡环境轨：场景切换后即播放，所以漫画加载、直接读档和正式玩法三种入口都能听见；不再依赖只在漫画期间存在的额外轨道。
- 第四关警报提升至独立的 −8 dB 场景电平，BGM 整体退到 −15 dB，为环境叙事留出空间；MP3/Ogg 循环支持在运行时显式启用。

## 2026-09-15 — 人物落地、石壁残影与窑炉重制

- NPC 皮肤接入时按首帧透明边距计算可见脚底，并额外压入地面 2 像素；主角动画也统一下沉 2 像素，减少人物浮空感。
- 第二关石壁突破后同步隐藏三条裂纹线和冷热覆盖层，不再残留橙色条纹。
- 第二关窑炉重制为朝右开口的横向砖窑，使用真实透明背景并按可见底边贴地；场景构建器也会自动忽略道具图片的透明底部留白。
- 第五关传单阿姨改用高阈值透明裁切并强制实体像素为不透明，清除雾化般的半透明观感。
- 完成第二、第五关实机截图复核，人物脚底、窑炉底座、石壁清除效果及阿姨不透明度均通过专项测试。

## 2026-09-15 — 第四关偶发退出修复

- 根据编辑器堆栈确认，第三关终点的 `Area2D.body_entered` 在物理回调中同步执行场景切换，导致旧场景的碰撞节点被违规释放；转场入口现统一延迟到空闲帧执行。
- `LevelManager` 增加切关互斥锁，同一帧重复收到终点信号时只推进一次，避免从第三关越过第四关或让刚创建的第四关再次被释放。
- 第四关目标提示在等待漫画期间持续验证自身、目标场景和 SceneTree 的有效性；若场景已切走便安全结束，不再访问空对象。
- 场景插画触发区改用 `set_deferred()` 停止碰撞监测，清除物理信号中的非法状态修改。
- 新增第三关至第四关的物理帧双触发专项回归，验证单次推进、安全进入第四关以及提示协程存活；原叙事、其他关卡和第一关重试测试同步通过。

## 2026-09-15 — 深接地与第一关石扣流程回归

- 主角和所有 NPC 的可见鞋底由压入地面 2 像素提高到 6 像素，使人物真正占用一小部分路面，不再贴着地表边缘。
- 第一关历史门牌按不透明像素底边压到地面下 7 像素；第二关窑炉及场景构建器中的大型摆件统一使用更深接地线。
- 第一关恢复可见的古墙石扣：初始时出口传送门完全隐藏，玩家踩下石扣后石扣下沉、追逐结束并在前方唤醒传送门。
- 石扣不再直接切关；玩家需要继续向右走入已经显现的传送门才进入第二关，保留完整的发现与穿越节奏。
- 实机复核门牌、石扣、传送门、人物和窑炉的接地效果；视觉、难度、石扣门控及第一关失败重试回归全部通过。

## 2026-09-15 — 第二关小道具复位与第五关阿姨重制

- 第二关木头和壶撤销自动可见底边接地，恢复各自原始局部坐标，避免小型互动道具被压进路面；窑炉继续保留深接地处理。
- 第五关传单阿姨依据既有三视图与主角像素密度重新生成，保留花头巾、红花上衣、黑色刺绣围裙、米色腰带和斜挎包等身份特征，改为更自然的左向侧身比例。
- 新素材使用真实透明通道，不再套用第一关追逐阿姨的强制不透明着色器；替换仅限第五关，第一关追逐动画保持不变。
- 第五关专用显示高度微调至 72 像素，实机中与主角体量协调并继续压入地面；第二关物品与第五关人物均完成 640×360 实机截图复核。

## 2026-09-15 — 小道具贴地与第五关阿姨落脚线校正

- 根据试玩反馈，第二关木头和壶恢复为按不透明像素底边贴地的版本；透明留白不参与计算，两件道具统一落在 247 像素地面线上。
- 修正第五关专用阿姨放大后的居中基准：基准偏移现在随 72 像素目标高度变化，不再沿用 60 像素角色的固定半高，鞋底上提 6 像素并与主角落脚线一致。

## 2026-09-15 — 渝灯互动精灵

- 新增左上角常驻的“渝灯”互动精灵：以重庆灯笼、吊脚楼屋檐、红穗和两江水纹为识别元素，使用 Godot 原生绘制，保持与现有像素场景的暖金/深青配色。
- 每次进入关卡自动显示一段对应年代和目标提示；点击精灵或按 E 可收起/展开对话框，正式人物对话期间自动隐藏，避免遮挡对白与抢占输入。
- 新增 `test_companion_v1.gd` 覆盖左上安全区、自动提示、第二关背景文案及人物对白互斥；实机 640×360 复核通过。

## 2026-09-16 — 渝灯正式像素重制与可拖动布局

- 将程序绘制占位替换为正式四帧像素悬浮动画；形象保留灯笼核心，并强化吊脚楼飞檐、红穗和两江蓝色飘带，描边、像素密度与暖色体积感按现有角色素材统一。
- 互动精灵默认移至右上安全区，对话框朝左展开；鼠标短点仍切换对话框，按住精灵移动则可拖动，松手后以归一化位置保存，窗口尺寸变化时自动限制在安全边界内。
- `generate2dsprite` 严格检查通过：4 帧均无空帧、源/输出边缘接触或粘贴裁切，角色尺度变异系数 0.0043；右上布局、动画、拖动入口、对白互斥与实机 640×360 画面均通过复核。

## 2026-09-16 — 渝灯自动讲解与互动问候

- 每次进入关卡后，渝灯无需点击便会自动依次说出玩法提示和两条对应时代的重庆背景；五关分别补充洪崖洞吊脚楼、磁器口水陆码头、开埠货运、抗战防空洞及当代山城记忆。
- 自动讲解使用一次性分段轮播，不无限重复打扰玩家；正式人物对白出现时会暂停并让出画面，对白结束后继续尚未讲完的内容。
- 点击精灵改为随机问候，拖动松手后会回应新位置；按 E 同样可以呼出问候，不再把点击用于收起说明。
- 右上角对话框由 244×78 缩至 216×64，正文由 11 号缩至 9 号，操作提示缩至 8 号；640×360 实机截图确认不遮挡主要玩法区域。
- 更新渝灯专项回归，覆盖进关自动提示、时代科普轮播、点击/拖动问候、紧凑尺寸和正式对白互斥，全部通过。

## 2026-09-16 — 实时照片时间戳与临时爆炸照明

- 第五关选择拍照时读取玩家本机时间，将“年月日、时分秒、重庆”写入洪崖洞照片；拍摄时刻进入剧情存档，片尾小相框继续显示同一次快门的紧凑时间戳，而不是重新取当前时间。
- 第四关每次导弹撞击新增橙黄色火花、爆心发光层与真正参与 2D 场景照明的径向暖光，瞬间照亮落点附近的挡板、地面和洞壁。
- 爆炸亮光约 0.46 秒指数衰减，火花寿命约 0.58 秒，整个反馈容器在 1 秒后自动销毁；不会永久改变 CanvasModulate，也不会留下光源节点。
- 导弹挡板专项与五关难度/双结局回归全部通过；640×360 实机截图确认实时日期可读、临时照明范围自然且结束后恢复黑暗。

## 2026-09-16 — 落地爆炸重构与照明扩圈

- 移除偏 UI 感的放射尖角爆炸，重构为紧贴地面的双层火团、横向冲击波、向上散射火星、碎屑和短暂烟尘，爆心底边与地面接触线一致。
- 无遮挡落弹的径向点光源纹理缩放由 4.4 提升至 8.8，实际照明直径扩大一倍；挡板命中也按相同比例由 3.6 提升至 7.2。
- 扩大范围但不延长闪光时间：暖光仍在约 0.46 秒衰减，整个火花/烟尘/光源容器仍于 1 秒后销毁并恢复洞内原始暗度。
- 导弹专项回归验证贴地火团、冲击波、烟尘、双倍照明范围和自动销毁全部通过，实机截图未出现大范围过曝。

## 2026-09-16 — 爆炸接触线校正

- 沿用人物与建筑的可见底边接地方法，将爆炸反馈从物理判定点继续压向真实接触面：地面命中下压 18 像素，挡板命中下压 4 像素。
- 火团底边、横向冲击波、火星发射点、烟尘和暖光中心作为同一反馈容器同步移动，避免火焰贴地后光源仍悬在半空。
- 只调整视觉接触线，不改变爆炸半径、伤害判断、挡板判定和一秒自动销毁逻辑；专项测试与 640×360 实机复核通过。
